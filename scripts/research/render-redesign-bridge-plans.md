# Render-redesign bridge plans (verified 2026-07-24, workflow wf_1a90bfab)

Ground-truthed against 26.1-snapshot-10 + 26.2 jars by 7 trace agents, each adversarially
verified. LightTexture + RenderType getters SHIPPED in 1.3.0-snapshot.3; the rest are
verified implementation plans for the next session(s), ranked by corpus impact.

## net/minecraft/client/resources/model/BakedModel (class_1087) - the baked model interface / block model access pipeline
- fate: mixed  bridgeable: True  verdict: PARTIAL
- epoch: Pre-26.1 for the BakedModel deletion itself: class_1087/BakedModel is already absent from 26.1-snapshot-10.jar (removed in the upstream 1.21.5 model rework; 26.1 already has the BlockStateModel/BlockModelPart pair). 26.2 then breaks the REPLACEMENT again: BlockStateModel moved renderer/block/model -> renderer/block/dispatch, BlockModelPart renamed to dispatch/BlockStateModelPart, hasTranslucency()Z replaced by materialFlags()I + hasMaterialFlag(I)Z, BakedQuad moved renderer/block/model -> resources/model/geometry, Material moved renderer/block/model -> resources/model/sprite, QuadCollection moved resources/model -> resources/model/geometry, and BlockRenderDispatcher (the lookup entry point) deleted entirely.
- oldApi: Interface net/minecraft/client/resources/model/BakedModel = class_1087. 25/90 corpus mods reference it. Members actually used (5-jar sample: supplementaries, lootr, waystones, carryon, VisualWorkbench; 48 referencing classes): method_4707 getQuads(Lclass_2680;Lclass_2350;Lclass_5819;)Ljava/util/List; returning List<class_777 BakedQuad> (~30 call sites, dominant), method_4711 getParticleIcon()->TextureAtlasSprite (~15), method_4712 isGui3d (x5, VisualWorkbench picks item transforms with it), method_4709 getTransforms->ItemTransforms (x3), method_4710 getOverrides->ItemOverrides (x2), method_24304 usesBlockLight (x2), method_4713 isCustomRenderer (x2), method_4708 useAmbientOcclusion (x1). Usage is BOTH implement and call: supplementaries implements class_1087 in 9 delegating wrapper models, lootr in 3 (those also implement Fabric FRAPI FabricBakedModel/emitBlockQuads), waystones/carryon/VisualWorkbench only consume (fetch model, call getters, pass to vanilla renderer). Model acquisition: ModelManager (class_1092) / BlockRenderDispatcher.getBlockModel (class_776.method_3349). Render sink: class_778.method_3367 = ModelBlockRenderer.renderModel(PoseStack$Pose, VertexConsumer, BlockState, BakedModel, FFF, II) (waystones RuneRenderUtil). Quad internals are NOT inspected in the content-mod sample (only class_777.method_3358 getDirection once); rendering-engine mods (sodium 11 classes, iris, ferritecore, continuity) do touch class_777 internals.
- replacement: The single interface split into a two-level pair. 26.2: net/minecraft/client/renderer/block/dispatch/BlockStateModel { collectParts(Lnet/minecraft/util/RandomSource;Ljava/util/List;)V, particleMaterial()Lnet/minecraft/client/resources/model/sprite/Material$Baked;, materialFlags()I } + net/minecraft/client/renderer/block/dispatch/BlockStateModelPart { getQuads(Lnet/minecraft/core/Direction;)Ljava/util/List; -> List<net/minecraft/client/resources/model/geometry/BakedQuad>, useAmbientOcclusion()Z, particleMaterial(), materialFlags()I }. Member map: getQuads(state,dir,random) -> collectParts(random,list) then per-part getQuads(dir) (BlockState param dropped); getParticleIcon() -> particleMaterial().sprite() (Material$Baked record wraps TextureAtlasSprite); useAmbientOcclusion -> same name on Part; usesBlockLight/isGui3d/isCustomRenderer -> no equivalent (item pipeline replaced by renderer/item/ItemModel + ItemStackRenderState); getTransforms -> ItemTransforms survives (26.1 renderer/block/model/ItemTransforms, 26.2 resources/model/cuboid/ItemTransforms); getOverrides -> ItemOverrides deleted in both jars. Lookup: 26.1 BlockRenderDispatcher.getBlockModel(BlockState)->BlockStateModel or ModelManager.getBlockModelSet(); 26.2 BlockRenderDispatcher deleted, use ModelManager.getBlockStateModelSet()Lnet/minecraft/client/renderer/block/BlockStateModelSet; then get(BlockState). Vanilla Part impl: resources/model/SimpleModelWrapper(QuadCollection, boolean, Material$Baked); vanilla model impl: dispatch/SingleVariant(BlockStateModelPart). Render sink: only ModelBlockRenderer.tesselateBlock(BlockQuadOutput,FFF,BlockAndTintGetter,BlockPos,BlockState,BlockStateModel,J) in both jars; the standalone Pose+VertexConsumer renderModel is gone.
- plan: Bridgeable for the content-mod majority via a synthesized per-mod embedded interface (SyntheticEmbedder pattern): retarget the class_1087 mapping to com/retromod/embedded/<mod>/LegacyBakedModel, an ASM-generated interface reproducing the old 8-method surface with default methods over a wrapped dispatch/BlockStateModel. Per member: getQuads(state,dir,random) = converting redirect into collectParts(random, tmp) + concat part.getQuads(dir) (state arg popped); getParticleIcon() = method redirect + return unwrap chain particleMaterial().sprite() (exactly the field-hop-accessor shape); useAmbientOcclusion() = delegate to first part; isGui3d()/usesBlockLight()/isCustomRenderer() = neutralize to constants (true/true/false, their item-pipeline consumers are dead anyway); getTransforms() = static accessor to surviving ItemTransforms (26.2 resources/model/cuboid); getOverrides() = neutralize (ItemOverrides deleted, corpus has only 2 pass-through call sites). Acquisition sites: class_776.method_3349 getBlockModel -> converting redirect to ModelManager.getBlockStateModelSet().get(state) wrapped in the synthetic (26.2; on 26.1 BlockRenderDispatcher.getBlockModel still exists, plain redirect). Implementors (supplementaries 9, lootr 3): reverse adapter, synthesized class implementing BlockStateModel+BlockStateModelPart delegating collectParts/getQuads(dir) -> old getQuads(null,dir,random) and particleMaterial() -> new Material$Baked(getParticleIcon(), false), materialFlags() -> 0; mechanically constructible, but wiring it into 26.x model LOADING (Fabric ModelLoadingPlugin surface, itself reworked) is separate Fabric-API-family work. Genuinely lost, not mechanical: (1) the standalone render sink class_778.method_3367 (Pose+VertexConsumer renderModel) has NO 26.x equivalent; needs a hand-written embedded polyfill that walks parts->quads and emits vertices (feasible: 26.2 BakedQuad exposes position(i)/packedUV(i)/direction()/materialInfo, and VertexConsumer+PoseStack survive in 26.2, but MultiBufferSource is deleted, so it depends on the buffer-source family bridge); (2) FRAPI FabricBakedModel/emitBlockQuads implementors (lootr, indium, continuity) are a separate Fabric-renderer-API family; (3) quad-internals mods (sodium, iris, ferritecore) are unbridgeable, 26.2 BakedQuad is a different data structure (Vector3fc positions + packed-long UVs vs old int[] vertices), already incompatible-list material.
- CORRECTED plan: Keep the claimed architecture (per-mod SyntheticEmbedder LegacyBakedModel interface wrapping the host BlockStateModel) with these corrections. (1) Synthetic generation is per-host-version: on 26.2 wrap net/minecraft/client/renderer/block/dispatch/BlockStateModel + BlockStateModelPart (materialFlags()I, sprite/Material$Baked); on 26.1 wrap net/minecraft/client/renderer/block/model/BlockStateModel + BlockModelPart (hasTranslucency()Z, no materialFlags, renderer/block/model/Material$Baked); same 8-method legacy surface, two generated bodies. (2) class_806 (ItemOverrides): class-redirect to an embedded com/retromod/embedded/<mod>/LegacyItemOverrides stub carrying public static final EMPTY (static-field-accessor covers the 13 corpus GETSTATIC field_4292 sites) and a neutralized resolve (method_3495) returning the input model; getOverrides() on the synthetic returns EMPTY. Do NOT plain-neutralize: mod descriptors retain the class_806 type and must resolve at verify time. (3) class_776 (BlockRenderDispatcher) is deleted on 26.2 and referenced by 14 corpus jars: class-redirect it to an embedded stand-in; redirect the Minecraft acquisition getter to a factory producing it; method_3349 delegates to Minecraft.getInstance().getModelManager().getBlockStateModelSet().get(state) wrapped in the synthetic (26.1: plain redirect, getBlockModel survives). (4) Per-member forward map as claimed: method_4707 = collectParts(random,tmp) + concat part.getQuads(dir), state popped, null dir safe (QuadCollection has an explicit null branch); method_4711 = particleMaterial().sprite(); method_4708 = first-part delegate; method_4712/method_24304/method_4713 = constants true/true/false; method_4709 = class-redirect class_809 to surviving ItemTransforms (26.2 resources/model/cuboid, 26.1 renderer/block/model), default NO_TRANSFORMS, accepting the item-display degradation. (5) Reverse adapter for implementors as claimed (collectParts/getQuads(dir) -> old getQuads(null,dir,random); particleMaterial() -> new Material$Baked(getParticleIcon(), false), ctor verified public; materialFlags() -> 0 on 26.2 / hasTranslucency() -> false on 26.1); note direct implementors are lootr 3 + supplementaries 1 + enhancedblockentities 1 + modernfix 1, with supplementaries' remaining models transitive via moonlight-lib, so the adapter must also cover library interfaces extending class_1087. (6) Genuinely-lost list must include ALL standalone Pose+VertexConsumer sinks, not just class_778.method_3367: also class_778.method_3374 (BiomesOPlenty, supplementaries) and class_776.method_3353/method_3355 (Cobblemon, supplementaries, chipped); all need the same hand-written embedded quad-walking polyfill and depend on the buffer-source family bridge (MultiBufferSource has 0 classes in 26.2; VertexConsumer and PoseStack survive). FRAPI implementors and quad-internals mods (sodium, iris, ferritecore, and note modernfix/enhancedblockentities are also implementors in this corpus) remain out of scope as claimed.
- verify: Direction and all load-bearing structural facts CONFIRMED by independent re-derivation from both jars: 26.2 dispatch/BlockStateModel + BlockStateModelPart descriptors match javap verbatim (collectParts(RandomSource,List)V, getQuads(Direction)->List<geometry/BakedQuad>, useAmbientOcclusion()Z, particleMaterial()->sprite/Material$Baked, materialFlags()I); Material$Baked is a public record (TextureAtlasSprite,boolean) with public sprite(), so both the forward hop and reverse-adapter ctor work; ModelManager.getBlockStateModelSet().get(BlockState) is public in 26.2; BlockRenderDispatcher.getBlockModel(BlockState)->BlockStateModel exists in 26.1 and the class is absent from 26.2; ModelBlockRenderer's only public render method in both jars is tesselateBlock(BlockQuadOutput,...); MultiBufferSource has 0 classes in 26.2; ItemTransforms survives at 26.2 resources/model/cuboid with NO_TRANSFORMS; BakedModel and all ItemOverride* classes absent from both jars; SimpleModelWrapper(QuadCollection,boolean,Material$Baked) and SingleVariant(BlockStateModelPart) ctors confirmed; QuadCollection.getQuads(Direction) has an explicit null branch (enumSwitch case -1) so null-cull-face semantics survive; 26.2 BakedQuad really is Vector3fc positions + packed-long UVs (quad-internals unbridgeability correct); corpus member-usage profile matches (method_4707 32 refs, method_4711 22, method_4708 9, method_4709 7, method_4712 6, method_24304 5, method_4713 5, method_4710 4; class_776.method_3349 in 8 jars; class_778.method_3367 in 4 jars incl. waystones). However the plan needs three corrections. (1) getOverrides "neutralize" is insufficient: 13 corpus classes (lootr 3, supplementaries 9, fancymenu 1) do GETSTATIC class_806.field_4292 (ItemOverrides.EMPTY) and implementors carry ()Lnet/minecraft/class_806; in method descriptors, so the deleted class_806 type must be class-redirected to an embedded stub with a static EMPTY, not merely call-site-neutralized; also "2 pass-through call sites" undercounts (4 sites in 3 jars, one of which, Cobblemon's ItemRendererMixin, calls class_806.method_3495 resolve). (2) The 26.1 pair is renderer/block/model/BlockStateModel + BlockModelPart (NOT BlockStateModelPart) with a different surface: hasTranslucency() on both, no materialFlags(), and Material$Baked at renderer/block/model, so the synthetic wrapper must be generated per host version. (3) The genuinely-lost sink list is broader than method_3367: corpus also hits class_778.method_3374 (BiomesOPlenty, supplementaries), class_776.method_3353 renderSingleBlock (Cobblemon, supplementaries), and method_3355 (chipped), and on 26.2 the deleted class_776 receiver type itself (referenced by 14 jars) needs an embedded stand-in plus an acquisition-getter redirect, which the plan glosses over. Implementor tally also off as stated: direct class_1087 implementors are lootr 3, supplementaries 1, enhancedblockentities 1, modernfix 1 (supplementaries' 9 are likely transitive via moonlight-lib's CustomBakedModel, its 9 block_models classes reference field_4292).
- evidence: 26.2 javap: "public interface net.minecraft.client.renderer.block.dispatch.BlockStateModelPart { public abstract java.util.List<net.minecraft.client.resources.model.geometry.BakedQuad> getQuads(net.minecraft.core.Direction); public abstract boolean useAmbientOcclusion(); public abstract net.minecraft.client.resources.model.sprite.Material$Baked particleMaterial(); public abstract int materialFlags(); }" and "public interface ...dispatch.BlockStateModel { public abstract void collectParts(net.minecraft.util.RandomSource, java.util.List<...BlockStateModelPart>); ... }". 26.1 javap: same pair at renderer/block/model with "public abstract boolean hasTranslucency();" and "public net.minecraft.client.renderer.block.model.BlockStateModel getBlockModel(net.minecraft.world.level.block.state.BlockState);" on BlockRenderDispatcher (class absent from 26.2 listing). Material$Baked (both jars): "private final net.minecraft.client.renderer.texture.TextureAtlasSprite sprite; ... public net.minecraft.client.renderer.texture.TextureAtlasSprite sprite();". 26.2 ModelManager: "public net.minecraft.client.renderer.block.BlockStateModelSet getBlockStateModelSet();". Corpus implement-side (lootr BarrelModel$BakedBarrelModel): "implements net.minecraft.class_1087,net.fabricmc.fabric.api.renderer.v1.model.FabricBakedModel" with "invokeinterface ... net/minecraft/class_1087.method_4707:(Lnet/minecraft/class_2680;Lnet/minecraft/class_2350;Lnet/minecraft/class_5819;)Ljava/util/List;". Consumer render si

## net/minecraft/client/renderer/LightTexture (intermediary class_765): lightmap coords + light texture management
- fate: mixed  bridgeable: True  verdict: PARTIAL
- epoch: 26.1. LightTexture is already absent from 26.1-snapshot-10 (unzip -Z1 grep -i lighttexture: zero hits), and BOTH replacements already exist there identically: 26.1 javap LightCoordsUtil shows FULL_BRIGHT=15728880, pack/block/sky, the smooth* family; 26.1 Lightmap already has 'public static float getBrightness(net.minecraft.world.level.dimension.DimensionType, int)'. 26.2 only adds LightCoordsUtil$BrightnessGetter + getLightCoords overloads.
- oldApi: net/minecraft/client/renderer/LightTexture. Corpus (90 Fabric 1.21.1 jars, constant-pool scan; 21 mods touch the name, 9 call members directly): static pack(II)I / method_23687 = 7 mods, 14 refs (Cobblemon, Debugify, EntityTextureFeatures, Indium, Litematica, Sodium, Supplementaries); instance turnOnLightLayer()V / method_3316 = 2 mods, 9 refs (Iris, physics-mod); static block(I)I / method_24186 and sky(I)I / method_24187 = 2 mods, 3 refs each (Sodium, Supplementaries); static getBrightness(Lnet/minecraft/world/level/dimension/DimensionType;I)F / method_23284 = 1 mod, 1 ref (Sodium). Zero Fieldrefs on class_765 anywhere: FULL_BRIGHT (field_32767, 0xF000F0) is a static final int and always compile-time inlined. Remaining touches are mixin refmap SIGNATURES that mention LightTexture only as a parameter type of LevelRenderer.renderLevel / ParticleEngine.render / renderSnowAndRain (carryon, fancymenu, notenoughanimations, xaero, iris, physics-mod; that is the LevelRenderer family, not LightTexture member usage), plus 2 mods that mixin INTO LightTexture itself: Iris (updateLightTexture/method_3313, calculateDarknessScale/method_42596) and DistantHorizons (updateLightTexture).
- replacement: Static coord math moved wholesale to net/minecraft/util/LightCoordsUtil: pack(II)I, block(I)I, sky(I)I with IDENTICAL names, descriptors, bit layout (block<<4 | sky<<20) and constants FULL_BRIGHT=15728880 (0xF000F0), FULL_SKY=15728640; 26.2 adds a smooth* family (smoothPack/smoothBlock/smoothSky/lightCoordsWithEmission) and getLightCoords(BlockAndLightGetter,BlockPos). getBrightness(Lnet/minecraft/world/level/dimension/DimensionType;I)F moved to net/minecraft/client/renderer/Lightmap as a static with the SAME descriptor. Texture management restructured into three classes: LightmapRenderStateExtractor.extract(LightmapRenderState,F)V computes (CPU side, successor of updateLightTexture; it keeps private calculateDarknessScale(LivingEntity,FF)F with the old signature), Lightmap.render(LightmapRenderState)V uploads (GpuTexture + UBO), Lightmap.getTextureView() exposes the GpuTextureView bound per-RenderPass; GameRenderer holds the instances (fields lightmap/uiLightmap/lightmapRenderStateExtractor, accessors lightmap()/levelLightmap() returning GpuTextureView). turnOnLightLayer/turnOffLightLayer are truly deleted with no functional analogue (zero string hits in all of 26.2): the global texture-unit-2 binding they performed no longer exists; vanilla passes bind the lightmap sampler themselves.
- plan: Everything the corpus actually CALLS is mechanically bridgeable with existing toolkit pieces; register these post-intermediary-remap (owner net/minecraft/client/renderer/LightTexture, which class_765 already maps to in intermediary-to-mojang.tsv): (1) method redirects, static, same descriptors: LightTexture.pack(II)I -> net/minecraft/util/LightCoordsUtil.pack(II)I; LightTexture.block(I)I -> LightCoordsUtil.block(I)I; LightTexture.sky(I)I -> LightCoordsUtil.sky(I)I (drop-in: bytecode-identical semantics). (2) method redirect, static, same descriptor: LightTexture.getBrightness(Lnet/minecraft/world/level/dimension/DimensionType;I)F -> net/minecraft/client/renderer/Lightmap.getBrightness. (3) method neutralize: LightTexture.turnOnLightLayer()V and turnOffLightLayer()V (pop receiver, no-op); safe because 26.x render passes bind the lightmap sampler themselves, so the old enable/disable calls have nothing left to do. (4) FULL_BRIGHT/FULL_SKY: nothing to register (always constant-inlined; zero corpus field refs); if a GETSTATIC ever surfaces, static field redirect to LightCoordsUtil.FULL_BRIGHT (same value). (5) bare type references: class redirect LightTexture -> net/minecraft/client/renderer/Lightmap so leftover descriptors/casts resolve (must be ordered AFTER the owner-specific member redirects in 1-3 so pack/block/sky land on LightCoordsUtil, not Lightmap). Genuinely lost: mixins INTO LightTexture (Iris updateLightTexture + calculateDarknessScale, DistantHorizons updateLightTexture) are not member-redirectable; the successor logic lives in LightmapRenderStateExtractor.extract/calculateDarknessScale(LivingEntity,FF)F but the handlers capture old NativeImage/GL internals and need re-authoring (both mods are deep-renderer, incompatible-list territory). Mixin-target signatures that merely take a LightTexture param (renderLevel/ParticleEngine.render) break because THOSE methods changed shape; that belongs to the LevelRenderer family, not this one.
- CORRECTED plan: Keep items 1, 2, 4, 5 unchanged (all verified drop-in): (1) static method redirects LightTexture.pack(II)I -> net/minecraft/util/LightCoordsUtil.pack(II)I, block(I)I -> LightCoordsUtil.block(I)I, sky(I)I -> LightCoordsUtil.sky(I)I; (2) static method redirect LightTexture.getBrightness(Lnet/minecraft/world/level/dimension/DimensionType;I)F -> net/minecraft/client/renderer/Lightmap.getBrightness (same descriptor, same math, verified); (4) nothing for FULL_BRIGHT/FULL_SKY (zero corpus field refs, constant-inlined; LightCoordsUtil fallback exists if ever needed); (5) class redirect LightTexture -> net/minecraft/client/renderer/Lightmap for bare type refs. Drop the ordering caveat in item 5: RetromodTransformer keys member redirects on the pre-class-redirect owner (visitMethodInsn, line 2974), so registering the class redirect alongside the member redirects is already safe with no ordering requirement. AMEND item 3: neutralizing turnOnLightLayer()V/turnOffLightLayer()V is semantically right (the global texture-unit binding no longer exists) but is unreachable on its own, because every corpus call site (iris x2, physics-mod x7, all 11 method_22974 refs) first calls GameRenderer.lightTexture()Lnet/minecraft/client/renderer/LightTexture; (intermediary class_757.method_22974), which is gone in 26.2 (GameRenderer keeps only a PRIVATE Lightmap field plus lightmap()/levelLightmap() returning GpuTextureView) and is bridged nowhere in Retromod today. Pair the neutralize with a companion bridge registered on owner net/minecraft/client/renderer/GameRenderer: method redirect lightTexture()Lnet/minecraft/client/renderer/LightTexture; -> an embedded reflective static helper (existing embedded-polyfill mechanism, per the opcode-aware field static bridge pattern) that reads GameRenderer's private lightmap field and returns the Lightmap instance; this stays type-correct because item 5's class redirect rewrites the mod-side descriptor to Lightmap. (Returning null would also satisfy the verified corpus uses, since every retrieved instance only ever receives the neutralized turnOnLightLayer call, but the reflective field read is the safe general form, e.g. iris CustomTextureManager also holds the instance.) File that companion under the GameRenderer family if families are tracked separately, but it must ship with this one for item 3 to ever execute. Unchanged: mixins INTO LightTexture (Iris updateLightTexture/calculateDarknessScale, DistantHorizons updateLightTexture) are non-bridgeable re-authoring territory, and LightTexture-typed params in changed mixin-target signatures belong to the LevelRenderer family.
- verify: The replacement mapping is fully correct on independent re-derivation from the 26.2 jar: LightCoordsUtil.pack/block/sky are bytecode-identical (block<<4|sky<<20; FULL_BRIGHT=15728880, FULL_SKY=15728640, lightCoordsWithEmission and getLightCoords(BlockAndLightGetter,BlockPos) all present); Lightmap.getBrightness(DimensionType,I)F is static with the exact old formula (lerp(dim.ambientLight(), (level/15)/(4-3*(level/15)), 1.0)); LightmapRenderStateExtractor.extract + private calculateDarknessScale(LivingEntity,FF)F confirmed; turnOn/turnOffLightLayer have zero string hits in all 26.2 classes; snapshot-10 parity holds. The tsv carries every needed member mapping (method_23687→pack, method_24186→block, method_24187→sky, method_23284→getBrightness, method_3315/3316→turnOff/OnLightLayer, class_2874→DimensionType). Constant-pool parsing of all 90 corpus jars confirms the usage set: pack (14 classes), block/sky (3), getBrightness (1, sodium), turnOnLightLayer (9), and ZERO field refs (FULL_BRIGHT inlining claim confirmed). The toolkit mechanics exist (neutralize pops the receiver, RetromodTransformer.java line 2911; member-redirect lookup is keyed on the pre-class-redirect owner, line 2974, so the ordering worry in item 5 is already handled by design). BUT the plan misses how the corpus actually reaches turnOnLightLayer: every one of the 9 call sites (iris IrisGenericRenderProgram/IrisLodRenderProgram, physics-mod x7) obtains the receiver via class_757.method_22974()Lclass_765; (GameRenderer.lightTexture()), javap-proven (invokevirtual class_757.method_22974 then invokevirtual class_765.method_3316). 26.2 GameRenderer has NO method returning Lightmap (only lightmap()/levelLightmap() returning GpuTextureView; the Lightmap field is private), and no existing Retromod shim bridges method_22974 (repo grep: zero hits). So plan item 3's neutralize is unreachable: those call sites die with NoSuchMethodError on lightTexture() one instruction earlier. The claim's sentence "Everything the corpus actually CALLS is mechanically bridgeable with existing toolkit pieces" is therefore false as written, though the affected mods (Iris, physics-mod) are deep-renderer incompatible-list territory, so practical severity is low. The non-bridgeable mixin slice (Iris/DH MixinLightTexture) is correctly identified.
- evidence: 26.2 javap -c LightCoordsUtil.pack: 'iload_0 / iconst_4 / ishl / iload_1 / bipush 20 / ishl / ior / ireturn' (= old block<<4|sky<<20); javap -constants: 'public static final int FULL_BRIGHT = 15728880; public static final int FULL_SKY = 15728640'; block: 'iload_0 / iconst_4 / ishr / bipush 15 / iand'. 26.2 javap Lightmap: 'public static float getBrightness(net.minecraft.world.level.dimension.DimensionType, int); public com.mojang.blaze3d.textures.GpuTextureView getTextureView(); public void render(net.minecraft.client.renderer.state.LightmapRenderState)'. 26.2 javap LightmapRenderStateExtractor: 'private float calculateDarknessScale(net.minecraft.world.entity.LivingEntity, float, float); public void extract(net.minecraft.client.renderer.state.LightmapRenderState, float)'. 26.2 javap GameRenderer: 'private final net.minecraft.client.renderer.Lightmap lightmap; private final net.minecraft.client.renderer.UiLightmap uiLightmap; public com.mojang.blaze3d.textures.GpuTextureView lightmap(); public com.mojang.blaze3d.textures.GpuTextureView levelLightmap()'. Byte scan of every 26.2 class for turnOffLightLayer/turnOnLightLayer/lightTexture strings: zero hits. 26.1-snapshot-10 javap shows the same LightCoordsUtil constants/members and the same Lightmap.getBrightness. Corpus refmap proof of the non-bridgeable slice: iris.refmap.json 'MixinLightTexture :: updateLightTexture -> Lnet/minecraft/class_765;method_3313(F)V' and 'calculateDarknessScale -> Lnet/minecraft/class_765;method_42596

## com/mojang/blaze3d/vertex/BufferUploader (intermediary net/minecraft/class_286)
- fate: restructured-bridgeable  bridgeable: True  verdict: PARTIAL
- epoch: 26.1 (or earlier): BufferUploader is already absent from 26.1-snapshot-10's com/mojang/blaze3d/vertex/ listing, and GpuDevice+RenderPass are already present in that jar (unzip -Z1 count = 2). NOT a 26.2 break. Separately, the adjacent Tesselator and VertexFormat$Mode classes DID break at 26.2 (present in 26.1-snapshot-10, absent in 26.2).
- oldApi: com/mojang/blaze3d/vertex/BufferUploader (class_286), 16 corpus mods. Members actually used (exact constant-pool refs): drawWithShader(Lcom/mojang/blaze3d/vertex/MeshData;)V = method_43433(Lnet/minecraft/class_9801;)V, 47 refs across 12 mods (BetterF3, Cobblemon, REI, ETF, FancyMenu, Iris, Litematica, ModMenu, Physics Mod, Supplementaries, Xaero Minimap+WorldMap); reset()V = method_34420, 6 refs across 4 mods (Iris, Litematica, Physics Mod, Sodium); draw(Lcom/mojang/blaze3d/vertex/MeshData;)V = method_43437, 3 refs across 2 mods (Xaero Minimap+WorldMap). Dominant call shape: RenderSystem.setShader(...) + setShaderTexture + blend toggles, Tesselator.getInstance().begin(Mode.QUADS, fmt), addVertex..., BufferUploader.drawWithShader(buildOrThrow()).
- replacement: No single member replacement; the class is gone and immediate drawing is restructured onto GpuDevice/CommandEncoder/RenderPass (already in 26.1). The 26.2 vanilla immediate-draw sequence for a MeshData (from DebugCrosshairRenderer, one of 6 vanilla MeshData+RenderPass consumers): GpuBuffer vb = RenderSystem.getDevice().createBuffer(Supplier, 32, mesh.vertexBuffer()); index via RenderSystem.getSequentialBuffer(PrimitiveTopology).getBuffer(indexCount) + .type():IndexType (or mesh.indexBuffer() when explicit); RenderPass pass = RenderSystem.getDevice().createCommandEncoder().createRenderPass(Supplier<String>, GpuTextureView color, Optional<Vector4fc> clearColor, GpuTextureView depth, OptionalDouble clearDepth); pass.setPipeline(RenderPipeline from net/minecraft/client/renderer/RenderPipelines constants); RenderSystem.bindDefaultUniforms(pass); pass.setVertexBuffer(0, vb.slice()); pass.setIndexBuffer(ib, indexType); pass.setUniform("DynamicTransforms", RenderSystem.getDynamicUniforms().writeTransform(new Matrix4f(modelView))); pass.drawIndexed(indexCount, instanceCount=1, firstIndex, 0, 0) [descriptor (IIIII)V]; pass.close(); vb.close(). MeshData itself survives unchanged at com/mojang/blaze3d/vertex/MeshData with vertexBuffer()/indexBuffer()/drawState(); DrawState is record(VertexFormat, vertexCount, indexCount, PrimitiveTopology, IndexType).
- plan: Per member: (1) drawWithShader(MeshData)V [12 mods, 47 refs]: static METHOD redirect, owner-only change, identical descriptor (MeshData survives at the same name in 26.2): INVOKESTATIC com/mojang/blaze3d/vertex/BufferUploader.drawWithShader(Lcom/mojang/blaze3d/vertex/MeshData;)V -> com/retromod/polyfill/minecraft/RetroBufferUploader.drawWithShader(same desc), embedded per-mod via SyntheticEmbedder. The polyfill replays the proven vanilla 26.2 sequence: read mesh.drawState() (record: format, vertexCount, indexCount, primitiveTopology, indexType - all survive); vb = RenderSystem.getDevice().createBuffer(sup, 32 /*usage flags from vanilla site*/, mesh.vertexBuffer()); ib+type = mesh.indexBuffer()!=null ? (upload, drawState.indexType()) : RenderSystem.getSequentialBuffer(drawState.primitiveTopology()).getBuffer(indexCount)/.type(); pass = getDevice().createCommandEncoder().createRenderPass(sup, mainRenderTarget().getColorTextureView(), Optional.empty(), getDepthTextureView(), OptionalDouble.empty()); pass.setPipeline(pipeline); RenderSystem.bindDefaultUniforms(pass); pass.setVertexBuffer(0, vb.slice()); pass.setIndexBuffer(ib, type); pass.setUniform("DynamicTransforms", RenderSystem.getDynamicUniforms().writeTransform(new Matrix4f(RenderSystem.getModelViewStack()))); pass.drawIndexed(indexCount, 1, 0, 0, 0); close pass, close vb. Pipeline selection: upgrade RemovedRenderStateNeutralize's setShader(Supplier)/setShaderTexture(I,Identifier)/setShaderColor(FFFF)/enableBlend etc. from neutralize to shadow-state RECORDERS (static bridges into the polyfill; corpus companion counts in the same classes: setShader 40, setShaderTexture 22, enableBlend 34), then map recorded shader+drawState.format() -> RenderPipelines constants which all exist public static final on 26.2: POSITION_COLOR+QUADS->RenderPipelines.GUI, POSITION_TEX(_COLOR)+QUADS->GUI_TEXTURED, lines->LINES, debug->DEBUG_QUADS/DEBUG_FILLED_BOX; recorded texture binds via pass.bindTexture(name, GpuTextureView, GpuSampler). Fallback with nothing recorded: choose by format alone. (2) draw(MeshData)V [2 mods, 3 refs]: same redirect to RetroBufferUploader.draw, same body (old no-shader-setup variant; modern path is identical). (3) reset()V [4 mods, 6 refs: sodium/iris/litematica/physics-mod, all heavy renderers]: method neutralize (args popped); no immediate VertexBuffer state exists anymore and the polyfill allocates per call, so reset is semantically empty. Host gating: register on 26.1+ hosts only (class absent from 26.1-snapshot-10 onward). ADJACENT DEPENDENCY for 26.2 hosts only: the corpus mesh-BUILDING path also breaks there - Tesselator (class_289: getInstance=method_1348 45 refs, begin=method_60827 44 refs) and VertexFormat$Mode (field_27382=QUADS, 40 refs) exist in 26.1-snapshot-10 but are gone in 26.2; they need their own family bridge (synthesized Tesselator wrapping new BufferBuilder(ByteBufferBuilder, PrimitiveTopology, VertexFormat) + static-field accessor Mode.X -> com/mojang/blaze3d/PrimitiveTopology.X, same constant names: QUADS/LINES/TRIANGLES/TRIANGLE_STRIP/TRIANGLE_FAN/DEBUG_LINES/DEBUG_LINE_STRIP/POINTS). Without that, drawWithShader never receives a MeshData on 26.2. Fidelity caveat: visual state (blend func, depth test) is baked into whichever RenderPipeline is chosen, not the mod's neutralized GL toggles, so exotic blend combos may render slightly differently; the dominant corpus shape (GUI-style textured/colored quads) maps cleanly.
- CORRECTED plan: Keep: fate=restructured-bridgeable, bridgeable=true; owner-only static method redirects for drawWithShader(MeshData)V and draw(MeshData)V into an embedded RetroBufferUploader; reset()V neutralize; the adjacent Tesselator/VertexFormat$Mode family bridge for 26.2 hosts; host gating 26.1+. Corrections: (A) Split the polyfill body per host line, selected on Retromod.TARGET_MC_VERSION: a 26.2 body exactly as claimed, and a 26.1 body re-derived from a 26.1 vanilla immediate-draw call site with the 26.1 signatures proven by javap on 26.1-snapshot-10: drawState().mode() (VertexFormat$Mode) not primitiveTopology(), getSequentialBuffer(VertexFormat$Mode), createRenderPass(Supplier, GpuTextureView, OptionalInt, GpuTextureView, OptionalDouble) with OptionalInt.empty() clear color, RenderPass.setVertexBuffer(int, GpuBuffer) (no slice), drawIndexed(IIII), writeTransform via the 4-arg (Matrix4fc, Vector4fc, Vector3fc, Matrix4fc) overload (pass identity extra/white color), and a 26.1-specific main-render-target accessor (GameRenderer.mainRenderTarget() is absent there; disassemble a 26.1 consumer, likely Minecraft.getMainRenderTarget(), to pin it). Re-verify the 26.1 body against release 26.1, not just snapshot-10. (B) Fix format->pipeline for formats with no GUI-suitable 26.2 pipeline: the only POSITION_TEX pipelines are GLINT/WORLD_BORDER/CELESTIAL, so instead of binding GUI_TEXTURED to a POSITION_TEX mesh (20 vs 24 byte stride, garbled), the polyfill re-strides vertex data during upload (it already copies mesh.vertexBuffer() into createBuffer): POSITION_TEX (12 corpus refs) -> POSITION_TEX_COLOR by appending 4 color bytes per vertex (recorded setShaderColor, else 0xFFFFFFFF) then GUI_TEXTURED; POSITION (6 refs) -> POSITION_COLOR then GUI; POSITION_COLOR (19 refs) -> GUI and POSITION_TEX_COLOR (11 refs) -> GUI_TEXTURED unchanged. NEW_ENTITY (field_1580, 3 refs) remains unmapped residual, log-and-skip. (C) Make the setShader path linkable before any recording can happen: register no-conversion converting redirects (the only redirect kind visitInvokeDynamicInsn applies to same-kind H_INVOKESTATIC bootstrap handles) for the six removed GameRenderer getters observed in the corpus (getPositionShader, getPositionColorShader, getPositionTexShader, getPositionTexColorShader, getParticleShader, getRendertypeArmorCutoutNoCullShader) onto embedded polyfill statics returning distinct marker instances, and class-redirect class_5944/CompiledShaderProgram to the embedded marker class so the indy instantiatedMethodType and the getter descriptors resolve (confirm the class redirect rewrites CONSTANT_MethodType bootstrap args; if not, extend visitInvokeDynamicInsn to resolveDescriptor on Type args). The markers then give deterministic shader->pipeline selection, with drawState().format() as the fallback. This also fixes a latent crash in today's RemovedRenderStateNeutralize path, which neutralizes the setShader INVOKESTATIC but leaves the lambda's handle to the removed getter to fail at linkage. (D) Unchanged fidelity caveats: blend/depth state is baked into the chosen pipeline, and per-call pass/buffer allocation plus mid-frame createRenderPass ordering relative to the retained GUI renderer remain in-game-verify items per the standard process.
- verify: The core verdict (restructured-bridgeable, bridgeable=true) and the entire 26.2-side derivation are CONFIRMED by independent re-derivation: BufferUploader absent from both jars (unzip -Z1); corpus counts exact (47 refs/12 mods method_43433 (Lnet/minecraft/class_9801;)V, 6/4 method_34420 with sodium/iris/litematica/physics-mod, 3/2 method_43437, companions 45/44/40/40/34/22 as claimed); TSV mappings exact; the 26.2 DebugCrosshairRenderer sequence verified instruction-for-instruction (bipush 32 = GpuBuffer.USAGE_VERTEX ConstantValue 32; createBuffer(Supplier,I,ByteBuffer); 5-arg createRenderPass with Optional<Vector4fc>/OptionalDouble; setVertexBuffer(I,GpuBufferSlice); setIndexBuffer(GpuBuffer,com/mojang/blaze3d/IndexType); setUniform("DynamicTransforms", writeTransform(new Matrix4f(Matrix4fStack))); drawIndexed(IIIII); render target via Minecraft.getInstance().gameRenderer.mainRenderTarget()); MeshData survives with the claimed accessors so the owner-only redirect is descriptor-valid; RenderPipelines/PrimitiveTopology constants and bindTexture(String,GpuTextureView,GpuSampler) + TextureManager.getTexture(Identifier)->AbstractTexture.getTextureView()/getSampler() all exist; Tesselator/Mode adjacent-dependency presence/absence verified. But the plan has three proven defects. (1) It registers ONE 26.2-shaped polyfill on "26.1+ hosts", yet 26.1-snapshot-10 javap shows different signatures at ~7 call sites: DrawState uses VertexFormat$Mode/VertexFormat$IndexType (no primitiveTopology()), getSequentialBuffer(VertexFormat$Mode), createRenderPass takes OptionalInt (packed ARGB) not Optional<Vector4fc>, DynamicUniforms has NO writeTransform(Matrix4f) (only the 4-arg Matrix4fc,Vector4fc,Vector3fc,Matrix4fc form), RenderPass.setVertexBuffer(int,GpuBuffer) not GpuBufferSlice, drawIndexed(IIII) not (IIIII), and GameRenderer.mainRenderTarget() does not exist on 26.1-snap-10 - the claimed body NoSuchMethodErrors on a 26.1 host. (2) The POSITION_TEX->GUI_TEXTURED pipeline mapping garbles vertex data: GUI_TEXTURED_SNIPPET is built with DefaultVertexFormat.POSITION_TEX_COLOR (24 B/vertex) while POSITION_TEX is 20 B/vertex, and the only POSITION_TEX pipelines in 26.2 are GLINT, WORLD_BORDER, CELESTIAL (none GUI-suitable); POSITION_TEX is the second-most-used format in the corpus's class_286-using classes (field_1585, 12 refs; also POSITION field_1592 6 refs has the same problem, GUI expects POSITION_COLOR). (3) The proposed setShader shadow-state recorder is unreachable: all 40 corpus setShader sites capture method refs to removed GameRenderer getters (45 MethodHandle constants: method_34540 getPositionColorShader x17, method_34542 getPositionTexShader x11, method_34543 getPositionTexColorShader x10, method_34539 x4, method_34501 x2, method_34546 x1) returning removed class_5944/CompiledShaderProgram; JVM indy linkage eagerly resolves the bootstrap Handle AND the instantiatedMethodType, so the mod crashes NoSuchMethodError/NoClassDefFoundError at the invokedynamic BEFORE setShader (neutralized or recorded) executes; RetromodTransformer.visitInvokeDynamicInsn (lines 2725-2782) only rewrites constructor and converting-redirect handles, and no existing shim redirects these getters (repo grep: only NeoForge_1_21_1_to_1_21_2 mentions CompiledShaderProgram).
- evidence: unzip -Z1 26.1-snapshot-10.jar blaze3d/vertex listing: has Tesselator.class, VertexFormat$Mode.class, NO BufferUploader.class; 26.2 listing has neither. Corpus constant-pool parse (owner net/minecraft/class_286): 47x METHOD method_43433 (Lnet/minecraft/class_9801;)V, 6x method_34420 ()V, 3x method_43437 (Lnet/minecraft/class_9801;)V; repo tsv: class_286=BufferUploader, class_9801=MeshData, method_43433=drawWithShader, method_34420=reset, method_43437=draw. 26.2 DebugCrosshairRenderer javap -c: "412: bipush 32 / 415: invokevirtual MeshData.vertexBuffer:()Ljava/nio/ByteBuffer; / 418: invokevirtual GpuDevice.createBuffer:(Ljava/util/function/Supplier;ILjava/nio/ByteBuffer;)Lcom/mojang/blaze3d/buffers/GpuBuffer;"; "127: invokevirtual GpuDevice.createCommandEncoder / 145: invokevirtual CommandEncoder.createRenderPass:(Ljava/util/function/Supplier;Lcom/mojang/blaze3d/textures/GpuTextureView;Ljava/util/Optional;Lcom/mojang/blaze3d/textures/GpuTextureView;Ljava/util/OptionalDouble;)Lcom/mojang/blaze3d/systems/RenderPass;"; "154: setPipeline / 159: invokestatic RenderSystem.bindDefaultUniforms:(Lcom/mojang/blaze3d/systems/RenderPass;)V / 172: setVertexBuffer:(ILcom/mojang/blaze3d/buffers/GpuBufferSlice;)V / 186: setIndexBuffer / 191: ldc String DynamicTransforms / 195: setUniform / 200: bipush 18, iconst_1, iconst_0, iconst_0, iconst_0 / 206: invokevirtual RenderPass.drawIndexed:(IIIII)V". javap RenderSystem (26.2): setShader/getShader/setShaderTexture/enableBlend ABSENT; getDevice, g

## net/minecraft/client/renderer/ItemBlockRenderTypes (intermediary class_4696): static block/fluid/item render-layer assignment
- fate: restructured-bridgeable  bridgeable: True  verdict: PARTIAL
- epoch: Already broken at 26.1: 26.1-snapshot-10 has no ItemBlockRenderTypes entry (unzip -Z1 finds only chunk/ChunkSectionLayer + ChunkSectionLayerGroup) and already carries the full replacement (ChunkSectionLayer{SOLID,CUTOUT,TRANSLUCENT}, rendertype/RenderTypes.solidMovingBlock, per-quad BakedQuad$SpriteInfo.layer). 26.2 only reshuffles the plumbing further: BakedQuad moved block/model -> resources/model/geometry with SpriteInfo renamed MaterialInfo, LiquidBlockRenderer.getRenderLayer replaced by FluidModel.layer(), and MultiBufferSource deleted. The bridge polyfill must therefore be version-aware between 26.1 and 26.2.
- oldApi: 1.21.1 class net/minecraft/class_4696 (ItemBlockRenderTypes), all static, returning class_1921 (RenderType). Corpus usage (12 of 90 jars, tallied via javap of every referencing class): method_23679 getChunkRenderType(BlockState) 13 refs in 5 mods (continuity, indium, iris, litematica, sodium); method_23683 getRenderType(BlockState,Z) 6 refs in 6 mods (BiomesOPlenty, Cobblemon, chipped, enhancedblockentities, litematica, supplementaries); method_23680 getRenderLayer(FluidState) 4 refs in 4 mods (litematica, sodium, xaerominimap, xaeroworldmap); method_29359 getMovingBlockRenderType(BlockState) 2 refs (supplementaries); method_23678 getRenderType(ItemStack,Z) 1 ref (supplementaries). No corpus mod touches setRenderLayer or the fancy-graphics flag.
- replacement: The class is gone in BOTH jars; the block-to-layer decision is no longer a static table at all. It is per-QUAD data derived from the model material's Transparency: ChunkSectionLayer.byTransparency(Transparency) -> enum {SOLID, CUTOUT, TRANSLUCENT} (net/minecraft/client/renderer/chunk/ChunkSectionLayer, identical 3 constants in both jars; old cutout_mipped/tripwire layers are collapsed away). Per-version read paths: 26.1: Minecraft.getModelManager().getBlockModelSet().get(BlockState) -> block/model/BlockStateModel.collectParts(RandomSource) -> BlockModelPart.getQuads(Direction) -> BakedQuad.spriteInfo().layer():ChunkSectionLayer; fluids via LiquidBlockRenderer.getRenderLayer(FluidState):ChunkSectionLayer (public, Map layerByFluid keyed on Fluid, default SOLID). 26.2: ModelManager.getBlockStateModelSet().get(BlockState) -> dispatch/BlockStateModel.collectParts(RandomSource,List) -> BlockStateModelPart.getQuads(Direction) -> resources/model/geometry/BakedQuad.materialInfo().layer(); fluids via ModelManager.getFluidStateModelSet().get(FluidState) -> FluidModel.layer(). SectionCompiler consumes the per-quad layer directly (getOrBeginLayer(Map,pack,ChunkSectionLayer)); chunk drawing uses ChunkSectionLayer.pipeline() (RenderPipeline), not RenderType. For non-chunk drawing, vanilla's own layer-to-RenderType conversion (26.2 MovingBlockFeatureRenderer) is: SOLID->RenderTypes.solidMovingBlock(), CUTOUT->cutoutMovingBlock(), TRANSLUCENT->translucentMovingBlock() (all present in BOTH jars at net/minecraft/client/renderer/rendertype/RenderTypes); items use Sheets.cutoutBlockItemSheet()/translucentBlockItemSheet() (both jars). Plain RenderType.solid()/cutout()/cutoutMipped()/translucent()/tripwire() no longer exist anywhere.
- plan: Mechanism: embedded reflective polyfill (add-polyfill + SyntheticEmbedder style) named e.g. com/retromod/polyfill/minecraft/RetroItemBlockRenderTypes, plus five owner/name/desc method redirects (all INVOKESTATIC -> INVOKESTATIC, same shapes, so plain method redirects suffice; no converting redirect needed). Return type resolves automatically: intermediary-to-mojang.tsv line 1487 already maps class_1921 straight to net/minecraft/client/renderer/rendertype/RenderType, and Common_1_21_11_to_26_1_ClassMoves covers the Mojang-name move. Registrations: (1) getChunkRenderType(BlockState)->polyfill: look up state's model (26.1 ModelManager.getBlockModelSet(), 26.2 getBlockStateModelSet(); detect via which getter exists), collectParts with a fresh RandomSource, take the first non-empty getQuads(null/Direction) quad's spriteInfo()/materialInfo().layer(), default SOLID (vanilla's own default), then map layer->RenderTypes.solidMovingBlock()/cutoutMovingBlock()/translucentMovingBlock(); cache per BlockState, invalidate on resource reload. (2) getRenderType(BlockState,Z) and (3) getMovingBlockRenderType(BlockState): same layer lookup -> same *MovingBlock mapping; this is byte-for-byte what vanilla 26.2 MovingBlockFeatureRenderer does, so the 6 BER/moving-block mods get vanilla-faithful buffers. (4) getRenderLayer(FluidState)->26.2: getFluidStateModelSet().get(fs).layer(); 26.1: replicate LiquidBlockRenderer's map (water/flowing_water->TRANSLUCENT via byTransparency of the water pipeline, lava->SOLID, default SOLID), then same layer->*MovingBlock mapping. (5) getRenderType(ItemStack,Z)->Sheets.cutoutBlockItemSheet()/translucentBlockItemSheet() by the item's block layer, mirroring 26.2 BakedQuad$MaterialInfo.of's item branch. Companion registration for identity consistency: mods that compare the result against old class_1921 statics (solid/cutout/translucent, now nonexistent) need those statics redirected to the SAME tokens (RenderTypes.solidMovingBlock() etc.) so == comparisons keep working. Caveats, honestly stated: (a) per-BlockState layer is no longer well-defined when one model mixes layers per quad; first-quad heuristic matches the common case. (b) On 26.2 MultiBufferSource.getBuffer is itself gone (replaced by SubmitNodeCollector; 26.1 still has MultiBufferSource$BufferSource), so BER-style consumers work end-to-end on 26.1 but on 26.2 depend on that separate family's bridge. (c) The 5 getChunkRenderType callers are chunk-meshing/pipeline mods (sodium, iris, indium, continuity, litematica); the bridge gives them a correct, identity-stable token, but sodium/iris/indium are deep-rendering mods that need far more than this family to run.
- CORRECTED plan: Keep the original plan (embedded reflective polyfill com/retromod/polyfill/minecraft/RetroItemBlockRenderTypes + five owner/name/desc INVOKESTATIC method redirects keyed post-remap on net/minecraft/client/renderer/ItemBlockRenderTypes.getChunkRenderType(BlockState)/getRenderType(BlockState,Z)/getMovingBlockRenderType(BlockState)/getRenderLayer(FluidState)/getRenderType(ItemStack,Z), layer->RenderTypes.solidMovingBlock()/cutoutMovingBlock()/translucentMovingBlock() mapping, Sheets.cutoutBlockItemSheet()/translucentBlockItemSheet() for the ItemStack overload, per-BlockState cache invalidated on resource reload, plus the companion redirect of old class_1921 solid/cutout/translucent statics to the SAME RenderTypes tokens so == stays consistent) with two fixes. FIX 1, model-set probe order: probe ModelManager.getBlockStateModelSet() FIRST (exists only on 26.2) and use it; only if absent fall back to getBlockModelSet() (26.1, returns BlockStateModelSet). Do NOT probe getBlockModelSet first: it exists on BOTH versions but returns a different type on 26.2 (BlockModelSet, not BlockStateModelSet), so naive existence-detection miscasts. FIX 2, 26.1 fluid branch: do not replicate the water/lava layer table (it is not constant; 26.1's LiquidBlockRenderer constructor computes layerByFluid via ChunkSectionLayer.byTransparency of the loaded water/lava sprite Transparency, so it varies with resource packs). Instead call the live host chain, all public and verified in the 26.1 jar: Minecraft.getInstance().getBlockRenderer() -> BlockRenderDispatcher.getLiquidRenderer() -> LiquidBlockRenderer.getRenderLayer(FluidState):ChunkSectionLayer, then apply the same layer->*MovingBlock token mapping. 26.2 fluid branch unchanged: getFluidStateModelSet().get(fs).layer(). All other registrations, the first-non-empty-getQuads heuristic with SOLID default (SOLID confirmed as vanilla's own default in both SectionCompiler and LiquidBlockRenderer), and the three stated caveats stand as written.
- verify: The fate (restructured-bridgeable) and every checkable replacement mapping CONFIRMED by independent re-derivation: ItemBlockRenderTypes absent from both jars; ChunkSectionLayer identical 3-constant enum {SOLID,CUTOUT,TRANSLUCENT} with public static byTransparency(Transparency) in both; 26.1 chain all public (Minecraft.getModelManager() -> getBlockModelSet():BlockStateModelSet -> get(BlockState) -> block/model/BlockStateModel default collectParts(RandomSource) -> BlockModelPart.getQuads(Direction) -> BakedQuad.spriteInfo().layer()); 26.1 fluid getRenderLayer(FluidState) public with layerByFluid Map.getOrDefault(SOLID) exactly as quoted (owner is net/minecraft/client/renderer/block/LiquidBlockRenderer, subpackage detail only); 26.2 chain verified (getBlockStateModelSet -> dispatch/BlockStateModel.collectParts(RandomSource,List) -> dispatch/BlockStateModelPart.getQuads -> geometry/BakedQuad.materialInfo().layer(); getFluidStateModelSet().get(fs):FluidModel.layer()); MovingBlockFeatureRenderer tableswitch maps ordinals 1/2/3 to RenderTypes.solidMovingBlock/cutoutMovingBlock/translucentMovingBlock, and all three are public static in BOTH jars, as are Sheets.cutoutBlockItemSheet/translucentBlockItemSheet (and MaterialInfo.of calls exactly those); RenderType has zero solid/cutout/cutoutMipped/translucent/tripwire members in either jar; MultiBufferSource present in 26.1, zero entries in 26.2. Corpus re-counted: 12 jars reference class_4696, per-method jar distribution 5/6/4/2/1 matches the claim, all INVOKESTATIC with the exact claimed descriptors ((class_2680), (class_2680;Z), (class_3610), (class_1799;Z) -> class_1921), and the 5 method_23679 callers are precisely sodium/iris/indium/continuity/litematica. tsv verified: line 1487 class_1921 -> net/minecraft/client/renderer/rendertype/RenderType; line 4346 class_4696 -> ItemBlockRenderTypes; method IDs 23678/23679/23680/23683/29359 map to the claimed Mojang names. PARTIAL only because the plan needs two corrections. (1) The 26.1 fluid branch as written ("replicate LiquidBlockRenderer's map: water/flowing_water->TRANSLUCENT, lava->SOLID") is subtly wrong: javap of the 26.1 LiquidBlockRenderer constructor shows layerByFluid is computed at bake time as ChunkSectionLayer.byTransparency(waterStill.transparency().or(waterFlowing.transparency())) from the loaded sprites, i.e. resource-pack-dependent, so a hardcoded replica diverges from the host's actual chunk layer under packs that change water transparency. Replication is also unnecessary: the plan missed a fully public live chain, Minecraft.getBlockRenderer() (public) -> BlockRenderDispatcher.getLiquidRenderer() (public) -> getRenderLayer(FluidState) (public), all verified in the 26.1 jar. (2) "Detect via which getter exists" has a miscast trap: 26.2 ModelManager has BOTH getBlockStateModelSet():BlockStateModelSet AND getBlockModelSet(), but the latter returns a DIFFERENT type on 26.2 (net/minecraft/client/renderer/block/BlockModelSet) than on 26.1 (where getBlockModelSet() returns BlockStateModelSet). The probe must check getBlockStateModelSet first (it exists only on 26.2) and fall back to getBlockModelSet on 26.1. Everything else in the plan (five INVOKESTATIC->INVOKESTATIC method redirects, layer->*MovingBlock mapping mirroring vanilla's own MovingBlockFeatureRenderer, Sheets item-sheet branch, companion class_1921-statics redirect to the SAME tokens for identity consistency, and all three stated caveats) survived adversarial checking unmodified.
- evidence: 26.1 jar: "public net.minecraft.client.renderer.chunk.ChunkSectionLayer getRenderLayer(net.minecraft.world.level.material.FluidState)" reading "Field layerByFluid ... Map.getOrDefault" with default "Field net/minecraft/client/renderer/chunk/ChunkSectionLayer.SOLID"; BakedQuad$SpriteInfo record: "private final net.minecraft.client.renderer.chunk.ChunkSectionLayer layer; ... public static ... of(...Material$Baked, com.mojang.blaze3d.platform.Transparency)". 26.2 jar: BakedQuad$MaterialInfo.of: "0: aload_1  1: invokestatic #34 // Method net/minecraft/client/renderer/chunk/ChunkSectionLayer.byTransparency:(Lcom/mojang/blaze3d/platform/Transparency;)Lnet/minecraft/client/renderer/chunk/ChunkSectionLayer;" then "invokestatic #69 // Method net/minecraft/client/renderer/Sheets.translucentBlockItemSheet:()...RenderType" / "#75 cutoutBlockItemSheet". ChunkSectionLayer.byTransparency: "Transparency.hasTranslucent -> TRANSLUCENT; hasTransparent -> CUTOUT; else SOLID". MovingBlockFeatureRenderer: tableswitch on ChunkSectionLayer.ordinal -> "58: invokestatic #139 // RenderTypes.solidMovingBlock()", "64: ... cutoutMovingBlock()", "70: ... translucentMovingBlock()" then "invokevirtual #170 // Method getVertexBuilder:(...RenderType)...VertexConsumer". SectionCompiler 26.2: "invokevirtual #167 // BlockStateModelSet.get:(...BlockState;)...dispatch/BlockStateModel" + "getOrBeginLayer:(Ljava/util/Map;...;Lnet/minecraft/client/renderer/chunk/ChunkSectionLayer;)". SectionCompiler 26.1: "invokevirtu

## net/minecraft/client/resources/model/ModelResourceLocation (class_1091) + its lookup pipeline (ModelManager.getModel, BlockModelShaper.stateToModelLocation)
- fate: mixed  bridgeable: True  verdict: PARTIAL
- epoch: 26.1 (already absent in 26.1-snapshot-10; that jar already has the full rework: Identifier, BlockStateModelSet, ItemModel, no BakedModel/BlockModelShaper/ItemModelShaper, so the break predates 26.2; 26.2 only reshuffled the new API: getBlockModelSet() renamed getBlockStateModelSet(), BlockStateModel moved renderer/block/model -> renderer/block/dispatch)
- oldApi: net/minecraft/client/resources/model/ModelResourceLocation = class_1091, a 1.21.1 record(id: class_2960/ResourceLocation, variant: String). Corpus usage (9 of 90 mods: 8 in bytecode + ferritecore via mixin JSON): ctor <init>(Lclass_2960;Ljava/lang/String;)V x8 (Cobblemon, lootr, modernfix; ferritecore @Injects it); record accessors comp_2875()=id() x7 (lootr, modernfix), comp_2876()=variant() x3 and method_4740()=getVariant() x2 (modernfix); equals/hashCode x3; MRL-typed fields/maps throughout supplementaries (16 classes), Cobblemon (6), VisualWorkbench, continuity, lootr. Consumers: class_1092.method_4742 = ModelManager.getModel(Lclass_1091;)Lclass_1087;(BakedModel) x10 in 4 mods (Cobblemon, lootr, modernfix, physics-mod); class_773.method_3340 = BlockModelShaper.stateToModelLocation(Lclass_2680;)Lclass_1091; x4 (VisualWorkbench, modernfix, physics-mod) and method_3336(Lclass_2960;Lclass_2680;) x1 (continuity); plus MRL-carrying mixins into class_1088 ModelBakery (method_61072/61074/61076, field_5394/field_5387/field_52276) and class_763 ItemModelShaper.method_3309 (modernfix, lootr, Cobblemon).
- replacement: No same-name or same-simple-name class anywhere in 26.1/26.2: the model-key concept was split by domain. Item models: net/minecraft/client/resources/model/ModelManager.getItemModel(Lnet/minecraft/resources/Identifier;)Lnet/minecraft/client/renderer/item/ItemModel; (both jars; note class_2960 ResourceLocation itself is renamed net/minecraft/resources/Identifier in 26.x). Block models (replaces stateToModelLocation+getModel two-step): 26.2 ModelManager.getBlockStateModelSet()Lnet/minecraft/client/renderer/block/BlockStateModelSet; then BlockStateModelSet.get(Lnet/minecraft/world/level/block/state/BlockState;)Lnet/minecraft/client/renderer/block/dispatch/BlockStateModel; (in 26.1-snapshot-10 the accessor is getBlockModelSet() and BlockStateModel is net/minecraft/client/renderer/block/model/BlockStateModel); 26.2 also adds BlockModelSet.get(BlockState)Lnet/minecraft/client/renderer/block/model/BlockModel;. Item-frame models (lootr's case): net/minecraft/client/resources/model/BlockStateDefinitions.getItemFrameFakeState(ZZ)LBlockState; + BlockModelResolver.updateForItemFrame(LBlockModelRenderState;ZZ)V. The old return type class_1087 BakedModel is also deleted from both jars, and BlockModelShaper (class_773) and ItemModelShaper (class_763) are deleted; there is no vanilla arbitrary-model-by-id lookup left (models load into Map<Identifier,UnbakedModel> but only blockstate/client-item-referenced ones are resolved via ModelDiscovery).
- plan: Per-mechanism, per-member (measured over the 90-jar corpus): (1) class_1091 TYPE: synthesized value class embedded per-mod (SyntheticEmbedder pattern, com/retromod/embedded/<mod-key>/ModelResourceLocation) registered via RetromodTransformer.registerSyntheticClass + class redirect net/minecraft/class_1091 -> synthetic. Shape: final fields id (net/minecraft/resources/Identifier, via the class_2960->Identifier redirect) + variant (String); ctor (Identifier,String) [8 corpus ctor sites]; comp_2875()->id [7 sites]; comp_2876()->variant [3 sites]; method_4740()->variant [2 sites]; equals/hashCode/toString matching the 1.21.1 record [3 sites]. The class redirect also rewrites every field/local/signature occurrence (supplementaries 16 classes, Cobblemon 6, VisualWorkbench, continuity, lootr all hold MRL-typed fields/maps; pure data, works as-is). (2) class_773.method_3340 (Lclass_2680;)Lclass_1091; [4 sites: VisualWorkbench, modernfix, physics-mod] and method_3336 (Lclass_2960;Lclass_2680;)Lclass_1091; [1 site: continuity]: method redirect INVOKESTATIC -> static helpers ON the synthetic that reproduce the 1.21.1 impl (block registry key + statePropertiesToString of state.getValues()), i.e. constructor-to-factory-style pure computation; deterministic, no host model API needed. Continuity's ImmutableMap<MRL,BlockState> and VisualWorkbench's Map<MRL,MRL> then work unchanged. (3) class_1092.method_4742 (getModel(MRL)->BakedModel) [10 sites in Cobblemon, lootr, modernfix, physics-mod]: NOT bridgeable within this family alone because the RETURN type class_1087/BakedModel is also deleted from both 26.x jars; the surviving lookups are ModelManager.getItemModel(Identifier)->ItemModel (item shape) and getBlockStateModelSet().get(BlockState)->BlockStateModel (block shape; 26.1 accessor name getBlockModelSet()). A converting redirect only becomes possible jointly with a BakedModel-family polyfill (wrap BlockStateModel/QuadCollection); note the block direction IS mechanically recoverable (the variant string parses back to a BlockState, exactly what modernfix's getBlockStatesForMRL does), so if/when a BakedModel wrapper family lands, method_4742 can become: parse synthetic MRL -> BlockState -> getBlockStateModelSet().get(state) -> wrap. Until then, method-neutralize (return null) is the soft-fail, which most callers here do not tolerate (they render with the result), so those features go inert. (4) Genuinely lost: (a) the ModelBakery/ItemModelShaper mixin surface: modernfix method_61072/method_61074 + fields field_5394/field_5387/field_52276 + class_763.method_3309, lootr method_61074, Cobblemon method_61076: target members do not exist in 26.x's restructured ModelBakery and ItemModelShaper is deleted; mechanism = mixin blocklist strip (pitfall 15), losing modernfix dynamic-resources and lootr/Cobblemon extra-model registration (re-author against 26.x ModelDiscovery/Fabric model API is the only real path); (b) ferritecore's ferritecore.mrl.mixin.json (ctor @Inject dedup): target class absent -> disable the config; harmless (memory optimization, obsolete without the class); (c) Fabric API ModelModifier$AfterBake$Context.topLevelId() (continuity) and Moonlight ClientHelper.getModel/SpecialModelEvent (supplementaries): third-party API families, out of scope here, same restructure applies. Net: 5 of 9 mods (supplementaries, VisualWorkbench, continuity, Cobblemon-data-side, ferritecore-neutralized) are covered by the synthetic MRL + stateToModelLocation redirects; the 4 mods calling getModel additionally need the BakedModel family before their render paths work.
- CORRECTED plan: Keep the original plan (synthetic per-mod ModelResourceLocation value class + class redirect; INVOKESTATIC method redirects for class_773.method_3340/method_3336 onto static helpers; method_4742 deferred until a BakedModel-family polyfill, with the parse-variant->BlockState->getBlockStateModelSet().get(state) recovery path; mixin blocklist for the ModelBakery/ItemModelShaper surface; disable ferritecore.mrl.mixin.json) with two corrections. (1) The stateToModelLocation helper MUST NOT call StateHolder.getValues(): its descriptor differs between the two 26.x jars (26.1-snapshot-10: ()Ljava/util/Map; returning Map<Property<?>,Comparable<?>>; 26.2: ()Ljava/util/stream/Stream; returning Stream<Property$Value<?>>), so one emitted shape link-fails on the other version. Build the variant string from the version-stable surface instead, identical descriptors in both jars: for each Property p in state.getProperties() (public Collection<Property<?>>, preserves definition order) append p.getName() + "=" + p.getName(state.getValue(p)) joined by commas (Property.getName()Ljava/lang/String; and the abstract getName(T)Ljava/lang/String; are public in both jars; getValue(Property) is public on StateHolder in both). This reproduces 1.21.1 statePropertiesToString output deterministically without touching getValues(). (2) When (or if) the method_4742 converting redirect lands, resolve the ModelManager block-model accessor by name AND descriptor, never name alone: 26.1-snapshot-10 has getBlockModelSet()Lnet/minecraft/client/renderer/block/BlockStateModelSet; while 26.2 has BOTH getBlockStateModelSet()Lnet/minecraft/client/renderer/block/BlockStateModelSet; and a same-named getBlockModelSet() that returns the DIFFERENT type Lnet/minecraft/client/renderer/block/BlockModelSet; (a name-only probe on 26.2 grabs the wrong class). Same caution for the returned model type, which moved packages: block/model/BlockStateModel (26.1) vs block/dispatch/BlockStateModel (26.2).
- verify: Every replacement mapping re-derived and confirmed byte-for-byte from both jars: no ModelResourceLocation/BakedModel/BlockModelShaper/ItemModelShaper class in 26.1-snapshot-10 or 26.2; ModelManager.getItemModel(Identifier)->ItemModel public in both; 26.2 getBlockStateModelSet()->BlockStateModelSet with public get(BlockState)->dispatch.BlockStateModel vs 26.1 accessor getBlockModelSet() and block/model/BlockStateModel (package split confirmed); 26.2-only BlockModelSet.get(BlockState)->BlockModel; BlockStateDefinitions.getItemFrameFakeState(ZZ) public static in both and 26.2 ItemFrameRenderer calls BlockModelResolver.updateForItemFrame:(LBlockModelRenderState;ZZ)V exactly as claimed; 26.2 ModelBakery has zero MRL-keyed members (topLevelModels/bakedTopLevelModels gone), validating the mixin-blocklist call; Identifier present / ResourceLocation absent in both. Corpus verified with digit-boundary-aware matching (naive substring matching over-matches method_4742x IDs): exactly the claimed 8 mods reference class_1091; the (Lclass_1091;)Lclass_1087; getModel descriptor is confined to exactly the 4 claimed mods (Cobblemon, lootr, modernfix, physics); method_3340 is INVOKESTATIC per VisualWorkbench disassembly (matching the planned static redirect); method_3336 only in continuity; ferritecore.mrl.mixin.json exists; tsv confirms all name mappings; helper deps (BuiltInRegistries.BLOCK public static final DefaultedRegistry<Block>, Registry.getKey(T)->Identifier, Property.getName()/getName(T)) all public in 26.2. The method_4742 non-bridgeability reasoning (return type BakedModel deleted) and the parse-variant-back recovery path both hold. PARTIAL, not CONFIRMED, for one wrong-descriptor landmine in the plan: the stateToModelLocation helper is specified as reproducing the 1.21.1 impl via state.getValues(), but StateHolder.getValues() returns Map<Property<?>,Comparable<?>> in 26.1-snapshot-10 and Stream<Property$Value<?>> in 26.2, so a helper emitted against either descriptor throws NoSuchMethodError on the other 26.x version. A version-stable surface exists and should be used instead. Secondary footnote: 26.2 ModelManager also has a getBlockModelSet() whose name collides with 26.1's accessor but returns a different type (BlockModelSet vs BlockStateModelSet), so accessor resolution must key on name+descriptor, never name alone. Minor non-substantive nits: claimed site counts run 1-2 above constant-pool entry counts (instruction vs pool counting, expected); the "5 of 9 mods" tally double-counts Cobblemon (8 distinct mods).
- evidence: Corpus 1.21.1 (javap -c): 'Method net/minecraft/class_1091."<init>":(Lnet/minecraft/class_2960;Ljava/lang/String;)V' (8 sites); 'Method net/minecraft/class_1092.method_4742:(Lnet/minecraft/class_1091;)Lnet/minecraft/class_1087;' (10 sites, 4 mods); 'Method net/minecraft/class_773.method_3340:(Lnet/minecraft/class_2680;)Lnet/minecraft/class_1091;' (4 sites); 'Method net/minecraft/class_1091.comp_2875:()Lnet/minecraft/class_2960;' (7); repo tsv confirms class_1091=ModelResourceLocation, method_4742=getModel, method_3340/3336=stateToModelLocation, method_4740=getVariant, class_1087=BakedModel, class_773=BlockModelShaper, class_763=ItemModelShaper. 26.x: 'unzip -Z1 | grep -i ModelResourceLocation' = 0 hits in BOTH 26.2 and 26.1-snapshot-10; grep BakedModel = 0 hits in both; grep BlockModelShaper/ItemModelShaper = 0 hits in both. 26.2 ModelManager javap: 'public net.minecraft.client.renderer.item.ItemModel getItemModel(net.minecraft.resources.Identifier);' and 'public net.minecraft.client.renderer.block.BlockStateModelSet getBlockStateModelSet();'; 26.2 BlockStateModelSet: 'public net.minecraft.client.renderer.block.dispatch.BlockStateModel get(net.minecraft.world.level.block.state.BlockState);'; 26.1 ModelManager: 'public net.minecraft.client.renderer.block.BlockStateModelSet getBlockModelSet();'. Item-frame proof (lootr's use case): 26.2 BlockStateDefinitions: 'public static ... BlockState getItemFrameFakeState(boolean, boolean);' and ItemFrameRenderer: 'invokevirtual ... Method

## net/minecraft/client/renderer/block/BlockRenderDispatcher + BlockModelShaper (block model lookup / single-block render)
- fate: mixed  bridgeable: True  verdict: PARTIAL
- epoch: Mixed, two-stage. At 26.1 (already absent in 26.1-snapshot-10 jar): BlockModelShaper class deleted, and BlockRenderDispatcher already stripped of renderSingleBlock/renderBatched/getBlockModelShaper/getModelRenderer/renderLiquid (only ctor, renderBreakingTexture, getModelSet, getLiquidRenderer, getBlockModel remained; getBlockModel already returned BlockStateModel, not BakedModel). At 26.2: the BlockRenderDispatcher class itself plus Minecraft.getBlockRenderer() and LiquidBlockRenderer deleted; lookup moved to ModelManager.getBlockStateModelSet().
- oldApi: 1.21.1 corpus (90 jars, intermediary): class_776=BlockRenderDispatcher referenced by 24 mods (102 classes), class_773=BlockModelShaper by 12 mods (30 classes). Members actually called: Minecraft.method_1541 getBlockRenderer()Lclass_776; (13 mods), class_776.method_3349 getBlockModel(Lclass_2680;)Lclass_1087; i.e. (BlockState)BakedModel (8 mods), class_773.method_3335 getBlockModel same desc (10 mods), class_776.method_3351 getBlockModelShaper()Lclass_773; (6 mods), class_776.method_3350 getModelRenderer()Lclass_778; ModelBlockRenderer (6 mods), class_773.method_3333 getModelManager (4 mods), class_776.method_3353 renderSingleBlock(Lclass_2680;Lclass_4587;Lclass_4597;II)V i.e. (BlockState,PoseStack,MultiBufferSource,int,int) (2 mods: Cobblemon, Supplementaries), renderBatched method_3355 (1), renderLiquid method_3352 (1), renderBreakingTexture (0).
- replacement: The state-to-model LOOKUP moved behind accessors: 26.2 chain is Minecraft.getModelManager() -> ModelManager.getBlockStateModelSet()Lnet/minecraft/client/renderer/block/BlockStateModelSet; -> BlockStateModelSet.get(Lnet/minecraft/world/level/block/state/BlockState;)Lnet/minecraft/client/renderer/block/dispatch/BlockStateModel; plus BlockStateModelSet.missingModel() and getParticleMaterial(BlockState)Lnet/minecraft/client/resources/model/sprite/Material$Baked; (particle sprite via .sprite()). BlockStateModelSet exists identically in 26.1 (there reachable as mc.getBlockRenderer().getModelSet() or ModelManager.getBlockModelSet()). Rename trap: 26.2 ModelManager.getBlockModelSet() still EXISTS but now returns a NEW class net/minecraft/client/renderer/block/BlockModelSet (state->BlockModel for entity rendering); the old object is behind the NEW name getBlockStateModelSet(). The BlockStateModel interface itself moved package block/model -> block/dispatch in 26.2. The RENDER half has no direct heir: renderSingleBlock died at 26.1 with the extract/submit pipeline; 26.2 vanilla path is BlockModelResolver.update(BlockModelRenderState, BlockState, BlockDisplayContext) then BlockModelRenderState.submit(PoseStack, SubmitNodeCollector, int, int, int) -> OrderedSubmitNodeCollector.submitBlockModel(PoseStack, RenderType, List<BlockStateModelPart>, int[], int, int, int). LiquidBlockRenderer: deleted in 26.2 (present 26.1). SectionBufferBuilderPack: unrelated, unchanged in both jars.
- plan: Lookup path (the bulk: 13+10+8+6 mod usages) is mechanically bridgeable with the existing toolkit; the immediate-render calls (2+1+1 mods) are not. Registrations: (1) class-redirect BOTH class_776 (BlockRenderDispatcher) and class_773 (BlockModelShaper) -> net/minecraft/client/renderer/block/BlockStateModelSet (same name+package in 26.1 and 26.2), collapsing the two owners onto one surviving object. (2) converting method redirect Minecraft.getBlockRenderer()Lnet/minecraft/client/renderer/block/BlockRenderDispatcher; -> INVOKESTATIC embedded polyfill RetroBlockModels.modelSet(Lnet/minecraft/client/Minecraft;)Lnet/minecraft/client/renderer/block/BlockStateModelSet; (receiver-to-arg widening); polyfill body version-gated: 26.2 mc.getModelManager().getBlockStateModelSet(), 26.1 mc.getModelManager().getBlockModelSet() (do NOT call getBlockModelSet on 26.2, it returns the new BlockModelSet type). (3) method redirect method_3349 AND method_3335 getBlockModel(BlockState)BakedModel -> BlockStateModelSet.get(Lnet/minecraft/world/level/block/state/BlockState;)L...BlockStateModel; (return-type conversion feeds the separate BakedModel->BlockStateModel family; getParticleIcon consumers hop to getParticleMaterial(state).sprite()). (4) method_3351 getBlockModelShaper -> identity passthrough via converting redirect to static RetroBlockModels.identity(BlockStateModelSet)BlockStateModelSet (both owners are now the same object). (5) method_3333 BMS.getModelManager -> static bridge returning Minecraft.getInstance().getModelManager(). Genuinely lost: method_3353 renderSingleBlock, method_3355 renderBatched, method_3352 renderLiquid; immediate tessellation into a mod-held MultiBufferSource has no 26.x equivalent (submit pipeline needs a SubmitNodeCollector the old call site does not have), so method-neutralize (args popped) them; the affected feature (Supplementaries CageItemRenderer block preview, Cobblemon single-block draws) goes inert, not crashy. method_3350 getModelRenderer (6 mods): ModelBlockRenderer survives in 26.2 but rebuilt (BlockModelLighter/QuadInstance internals, old tesselate-with-MultiBufferSource methods gone), so redirecting the getter alone is useless; neutralize/blocklist and treat as its own family.
- CORRECTED plan: Keep registrations (1)-(5) plus the neutralize list exactly as claimed, with these corrections. (6) NEW, required: handle class_773's STATIC methods method_3340 (Lclass_2680;)Lclass_1091; and method_3336 (Lclass_2960;Lclass_2680;)Lclass_1091; BEFORE the class_773 class-redirect applies, since redirecting the owner to BlockStateModelSet leaves INVOKESTATICs pointing at statics that do not exist there (hard NoSuchMethodError at first call). ModelResourceLocation (class_1091) exists in neither 26.1 nor 26.2, so no faithful bridge exists: route both to an embedded polyfill static (converting redirect) that returns null, or blocklist the call sites, and scope the real decision into the dead-ModelResourceLocation family (whatever that family synthesizes as a carrier type, these statics should construct it from Registry block id + state values; the 1.21.1 semantics are pure data: blockId + statePropertiesToString(state.getValues())). Affected mods: VisualWorkbench, modernfix, physics-mod (method_3340), continuity (method_3336); all but VisualWorkbench are deep-render mods likely on the incompatible list anyway. (7) NEW, explicit: method_3339 getParticleIcon (Lclass_2680;)Lclass_1058; -> converting redirect to polyfill static RetroBlockModels.particleIcon(BlockStateModelSet, BlockState) returning getParticleMaterial(state).sprite(); verified exact: sprite() returns TextureAtlasSprite = class_1058 (26.2 TerrainParticle bytecode). (8) Version-gating caveat strengthened: not only must the polyfill pick getBlockStateModelSet (26.2) vs getBlockModelSet (26.1), but every emitted descriptor touching BlockStateModel or Material$Baked differs per host (26.1: net/minecraft/client/renderer/block/model/BlockStateModel and net/minecraft/client/renderer/block/model/Material$Baked; 26.2: net/minecraft/client/renderer/block/dispatch/BlockStateModel and net/minecraft/client/resources/model/sprite/Material$Baked), so the method-redirect registrations for method_3349/method_3335 and the particle-icon polyfill internals must be selected by host version, or the polyfill must be written reflectively so one compiled body serves both. All other elements verified correct as claimed.
- verify: Every replacement mapping re-derived and confirmed from the jars: 26.2 deletes BlockRenderDispatcher and LiquidBlockRenderer (class list; 0 jar-wide hits for getBlockRenderer/renderSingleBlock/renderBatched/renderLiquid strings) while 26.1-snapshot-10 keeps both; 26.2 ModelManager has getBlockStateModelSet()->BlockStateModelSet AND getBlockModelSet()->NEW BlockModelSet (get(BlockState)->BlockModel, wraps a BlockStateModelSet fallback + BlockColors), so the rename trap is real; BlockStateModelSet.get/missingModel/getParticleMaterial verified in both jars; 26.2 TerrainParticle bytecode matches the quoted chain verbatim including Material$Baked.sprite(); the submit path (BlockModelResolver.update, BlockModelRenderState.submit, OrderedSubmitNodeCollector.submitBlockModel) verified with exact descriptors and no surviving MultiBufferSource path, justifying neutralize for method_3353/3355/3352; 26.2 ModelBlockRenderer confirmed rebuilt (tesselateBlock now takes BlockQuadOutput; BlockModelLighter/QuadInstance fields), justifying the method_3350 family split; all intermediary IDs and corpus counts (13/10/8/6 lookups, 2/1/1 renders, 6 getModelRenderer) reproduced. PARTIAL for two plan defects found by owner-scoped corpus enumeration: (a) class_773 has two STATIC members the plan never addresses, method_3340 stateToModelLocation(BlockState)ModelResourceLocation (VisualWorkbench, modernfix, physics-mod) and method_3336 stateToModelLocation(ResourceLocation,BlockState)ModelResourceLocation (continuity); ModelResourceLocation exists in NEITHER 26.1-snapshot-10 nor 26.2 (empty grep of both class lists), and the plan's blanket class_773->BlockStateModelSet class-redirect would rewrite these INVOKESTATICs onto a class with no such statics, turning them into hard NoSuchMethodErrors; (b) method_3339 getParticleIcon (3 mods: DistantHorizons, physics-mod, xaeroworldmap) is covered only parenthetically and needs an explicit registration. Also two accuracy notes: "BlockStateModelSet exists identically in 26.1" holds for the class name only, not descriptors (26.1 get() returns block/model/BlockStateModel and getParticleMaterial returns renderer/block/model/Material$Baked vs 26.2's block/dispatch/BlockStateModel and resources/model/sprite/Material$Baked), so BOTH halves of the bridge must be version-gated, not just the ModelManager getter name; and BlockModelRenderState lives at renderer/block/, not renderer/block/dispatch/.
- evidence: 26.1 javap BlockRenderDispatcher (class present, slimmed): "public net.minecraft.client.renderer.block.BlockStateModelSet getModelSet(); public net.minecraft.client.renderer.block.model.BlockStateModel getBlockModel(net.minecraft.world.level.block.state.BlockState);" and 26.1 Minecraft: "public net.minecraft.client.renderer.block.BlockRenderDispatcher getBlockRenderer();". 26.2: no BlockRenderDispatcher.class in unzip -Z1; strings getBlockRenderer/renderSingleBlock/blockRenderDispatcher = 0 classes jar-wide. 26.2 TerrainParticle bytecode (the old getBlockRenderer call site): "16: invokevirtual // Minecraft.getModelManager:()Lnet/minecraft/client/resources/model/ModelManager; 19: invokevirtual // ModelManager.getBlockStateModelSet:()Lnet/minecraft/client/renderer/block/BlockStateModelSet; 24: invokevirtual // BlockStateModelSet.getParticleMaterial:(...)Lnet/minecraft/client/resources/model/sprite/Material$Baked;". 26.2 BlockStateModelSet: "public net.minecraft.client.renderer.block.dispatch.BlockStateModel get(net.minecraft.world.level.block.state.BlockState);". 26.2 ModelManager: "public net.minecraft.client.renderer.block.BlockStateModelSet getBlockStateModelSet(); public net.minecraft.client.renderer.block.BlockModelSet getBlockModelSet();" (26.1 ModelManager had only "public net.minecraft.client.renderer.block.BlockStateModelSet getBlockModelSet();"). Corpus call sites: fallingleaves LeafUtil "invokevirtual // Method net/minecraft/class_310.method_1541:()Lnet/minecraft/cla

## net/minecraft/client/renderer/RenderType + RenderType$CompositeState + RenderStateShard (custom render type construction, class_1921 / class_4668)
- fate: mixed  bridgeable: True  verdict: CONFIRMED
- epoch: 26.1. The 26.1-snapshot-10 jar already contains net/minecraft/client/renderer/rendertype/RenderType + RenderSetup/RenderTypes and has ZERO RenderStateShard or CompositeState classes (unzip -Z1 grep), with create already package-private (String,RenderSetup)-only. A second wave hits at 26.2: MultiBufferSource(.class + $BufferSource), VertexFormat$Mode, RenderType.draw(MeshData), bufferSize(), mode() all present in 26.1 and absent in 26.2, which adds prepare()/primitiveTopology() instead; RenderSetupBuilder also loses bufferSize(int) at 26.2.
- oldApi: net/minecraft/client/renderer/RenderType (class_1921) and its CompositeState/CompositeRenderType inners, plus net/minecraft/client/renderer/RenderStateShard (class_4668) and its shard inners. Corpus (90 Fabric 1.21.1 jars): 34 mods reference class_1921 at all; 8 reference CompositeState; 9 reference RenderStateShard. Measured member usage (mods): translucent() 15, cutout() 12, entitySolid(RL) 10, cutoutMipped() 9, entityCutoutNoCull(RL) 8, entityCutout(RL) 7, setupRenderState()/clearRenderState() 7 each, guiOverlay() 6, entityTranslucent(RL) 6, RenderType.<init>(String,VertexFormat,Mode,I,Z,Z,Runnable,Runnable) super-called by 6 (iris 5x, ImmediatelyFast 2x, supplementaries 5x, fancymenu, xaerominimap, xaeroworldmap), CompositeState.builder chain 5 (Cobblemon, ImmediatelyFast, REI, ETF, iris + supplementaries/xaero/sound-physics via shard ctors), create(String,VertexFormat,Mode,I,Z,Z,CompositeState) 3 (Cobblemon 2x, iris, sound-physics) + create(String,VertexFormat,Mode,I,CompositeState) 1 (REI), format() 5, mode() 5, bufferSize() 4, text(RL) 4, gui() 4, affectsCrumbling() 3, waterMask() 3, tripwire() 3, solid() 5, isOutline() 2, draw(MeshData) 1.
- replacement: Class moved: net/minecraft/client/renderer/rendertype/RenderType (both 26.1 and 26.2). RenderType.create survives ONLY as package-private static RenderType create(String, RenderSetup); every public create overload and the CompositeState/RenderStateShard world is gone. New construction: RenderSetup.builder(RenderPipelines.X):RenderSetup$RenderSetupBuilder .withTexture("Sampler0", Identifier).useLightmap().useOverlay().affectsCrumbling().sortOnUpload().setLayeringTransform(LayeringTransform).setOutline(RenderSetup$OutlineProperty).createRenderSetup() then RenderType.create(name, setup). Static getters moved to net/minecraft/client/renderer/rendertype/RenderTypes (public static, e.g. entitySolid(Lnet/minecraft/resources/Identifier;)Lnet/minecraft/client/renderer/rendertype/RenderType;). Cull naming flipped: old entityCutout -> RenderTypes.entityCutoutCull(Identifier); old entityCutoutNoCull -> RenderTypes.entityCutout(Identifier). Block-layer getters (solid/cutout/cutoutMipped/translucent/tripwire) have NO RenderType successor: chunk layers are now enum net/minecraft/client/renderer/chunk/ChunkSectionLayer{SOLID,CUTOUT,TRANSLUCENT} whose pipeline() returns a RenderPipeline; nearest RenderType-typed values are RenderTypes.solidMovingBlock()/cutoutMovingBlock()/translucentMovingBlock(). Instance surface: format(), affectsCrumbling(), isOutline(), sortOnUpload(), outline() survive on both; 26.1 still has mode()/bufferSize()/draw(MeshData) but 26.2 deletes them (mode -> primitiveTopology():com.mojang.blaze3d.PrimitiveTopology; VertexFormat$Mode itself deleted in 26.2). Consumption path on 26.2: MultiBufferSource is deleted; RenderType flows into OrderedSubmitNodeCollector.submitCustomGeometry(PoseStack, RenderType, SubmitNodeCollector$CustomGeometryRenderer) and submitModel/submitBlockModel. setupRenderState/clearRenderState and the whole shard/Runnable GL state machine have no successor at all.
- plan: Bridgeable for the bulk of the corpus (the 34 getter users and the vanilla-shard subset of the 8 CompositeState users); truly lost for RenderType subclassers and custom Runnable shards. (1) Class redirect: net/minecraft/client/renderer/RenderType -> net/minecraft/client/renderer/rendertype/RenderType (the tsv already maps class_1921 to the new path; the stale inner-class lines class_1921$class_4688 -> RenderType$CompositeState must become polyfill targets instead). (2) Method redirects, owner RenderType -> RenderTypes, exact registrations: entitySolid(Identifier), entityTranslucent(Identifier), text(Identifier), waterMask(), lines(), crumbling(Identifier), eyes(Identifier), beaconBeam(Identifier,Z) keep names; entityCutout(Identifier) -> entityCutoutCull(Identifier); entityCutoutNoCull(Identifier) -> entityCutout(Identifier) (proven by withCull(false) on the ENTITY_CUTOUT pipeline); solid() -> solidMovingBlock(); cutout() and cutoutMipped() -> cutoutMovingBlock(); translucent() and tripwire() -> translucentMovingBlock() (honest approximations: block layers themselves are ChunkSectionLayer pipelines now); gui()/guiOverlay() are NOT this family, they belong to the Gui2D migration (no RenderType successor). (3) Method neutralize: setupRenderState()/clearRenderState() (7 mods, no GL state machine exists to set). (4) Instance members: format()/affectsCrumbling()/isOutline()/sortOnUpload() need nothing; on 26.2 bufferSize() -> static bridge returning RenderType.SMALL_BUFFER_SIZE, mode() -> embedded polyfill VertexFormat$Mode enum wrapping primitiveTopology() (Mode class is deleted in 26.2, so a converting redirect alone is not enough). (5) create(...CompositeState) (Cobblemon, REI, iris, sound-physics) + CompositeState builder chains: embed per-mod polyfill classes for CompositeState/CompositeStateBuilder/shard types that RECORD settings (texture id from TextureStateShard(Identifier,Z,Z), which vanilla shader constant from ShaderStateShard, transparency/cull/depth/lightmap/overlay flags), then a converting redirect from both old create descriptors to an embedded factory that maps the recorded shader constant to the matching RenderPipelines.X, replays flags onto RenderSetup.builder(pipeline)....createRenderSetup(), and invokes the package-private RenderType.create(String,RenderSetup) reflectively (Fabric/Knot loads MC without JPMS modules, so setAccessible works; same reflective-polyfill mechanism as the existing opcode-aware field bridges). This covers vanilla-shard compositions, which is what Cobblemon/sound-physics/xaero/REI do. GENUINELY LOST: (a) subclassing RenderType via the old 8-arg ctor (6 mods): the new ctor is private and the engine never calls user Runnables, so a synthesized superclass produces an object no vanilla API accepts as a working RenderType; iris/ImmediatelyFast/sodium are engine mods already on the incompatible list, but supplementaries/fancymenu/xaero lose those specific custom types; (b) custom Runnable-based shards (TexturingStateShard, EmptyTextureStateShard(Runnable,Runnable): supplementaries 2x, Cobblemon 1x) can only be recorded as no-ops, degrading that visual effect; (c) on 26.2 additionally RenderType.draw(MeshData) and the whole MultiBufferSource.getBuffer immediate path (deleted; replaced by OrderedSubmitNodeCollector submit* which has no old-shape equivalent), so mods that obtained a VertexConsumer from a buffer source need the separate MultiBufferSource-family bridge, not this one.
- CORRECTED plan: Plan is sound as written; apply three small corrections. (1) In the loss accounting, list the 6 subclassers as iris, ImmediatelyFast, fancymenu, supplementaries, xaerominimap, xaeroworldmap; sodium does not subclass RenderType (it only calls setup/clearRenderState and is handled by the neutralize step plus the incompatible list). (2) Add debugLineStrip(D) to the getter redirect table: no 26.2 successor exists in RenderTypes, so redirect to RenderTypes.lines() as an honest approximation (or synthesize via RenderSetup.builder over a line-strip-topology pipeline if one exists in RenderPipelines); note the recorder polyfill must also accept LineStateShard(OptionalDouble) and drop the width (no RenderSetupBuilder equivalent). (3) Count custom Runnable shards as supplementaries 1x (SlimedRenderTypes$OffsetTexturing) + Cobblemon 1x (PosableModel$DynamicStateShard) + iris 4x raw RenderStateShard subclasses; supplementaries' remaining Runnables arrive via the 8-arg RenderType ctor and are already covered by loss bucket (a).
- verify: Every load-bearing fact re-derived independently from the jars checks out. (1) Class move: both 26.1-snapshot-10 and 26.2 ship net/minecraft/client/renderer/rendertype/RenderType; zero RenderStateShard/CompositeState classes in either (26.2 unzip -Z1 grep RenderStateShard = 0). (2) javap proves create survives only as package-private "static RenderType create(String, RenderSetup)" with a private ctor, in BOTH jars. (3) The 26.1/26.2 epoch split is exact: 26.1 RenderType has draw(MeshData)/bufferSize()/mode():VertexFormat$Mode and the jar still has MultiBufferSource + VertexFormat$Mode.class; 26.2 deletes all of those and adds prepare()/primitiveTopology():com.mojang.blaze3d.PrimitiveTopology; format/isOutline/outline/affectsCrumbling/sortOnUpload survive on both; SMALL_BUFFER_SIZE is public static final. (4) RenderSetup.builder(RenderPipeline) is public static and the builder chain matches the claim verbatim (plus setOutputTarget/setTextureTransform/sampler overload the claim omitted); RenderSetup is pure data, no runnable/GL hooks, so "setupRenderState/clearRenderState have no successor" holds. (5) Every claimed RenderTypes getter exists with the exact descriptor on BOTH jars; no solid()/cutout()/cutoutMipped()/translucent()/tripwire() (grep = 0); solidMovingBlock/cutoutMovingBlock/translucentMovingBlock present. (6) Cull flip independently proven: RenderPipelines <clinit> builds pipeline/entity_cutout with iconst_0;withCull(Z) and pipeline/entity_cutout_cull without withCull; RenderTypes lambda$static$6 ("entity_cutout") uses ENTITY_CUTOUT, lambda$static$5 ("entity_cutout_cull") uses ENTITY_CUTOUT_CULL; old 1.21.1 entityCutout culled by default, so old entityCutout->entityCutoutCull / old entityCutoutNoCull->entityCutout is correct. (7) ChunkSectionLayer is enum {SOLID,CUTOUT,TRANSLUCENT} with pipeline():RenderPipeline; OrderedSubmitNodeCollector.submitCustomGeometry(PoseStack, RenderType, SubmitNodeCollector$CustomGeometryRenderer) exists. (8) tsv line 1487 already maps class_1921 to the new path; lines 1488-1491 + the class_4668 block (4320-4339) are stale old-path mappings, exactly as claimed. (9) Corpus counts verified by constant-pool scan of all 90 jars: 34 mods reference class_1921, exactly 8 reference CompositeState (the named set), exactly 7 reference method_23516/23518 (setup/clear), exactly 6 subclass class_1921 via the 8-arg ctor; sound-physics composes pure vanilla shard constants into method_24049 (the polyfill-recorder target case), Cobblemon has exactly 1 EmptyTextureStateShard(Runnable,Runnable) subclass. The genuinely-lost analysis (private ctor, engine never invokes user Runnables, no state machine) is structurally correct. Minor imprecisions, none verdict-changing: (a) sodium is NOT one of the 6 subclassers (it is only in the 7-mod setup/clear set); the six are iris, ImmediatelyFast, fancymenu, supplementaries, xaerominimap, xaeroworldmap; (b) supplementaries has 1 custom shard subclass (SlimedRenderTypes$OffsetTexturing extends TexturingStateShard), not 2; its other Runnables flow through the 8-arg RenderType ctor already counted in loss bucket (a); iris additionally has 4 raw class_4668 subclasses; (c) the redirect table needs one row the plan missed: RenderType.debugLineStrip(double) (method_49043, called by sound-physics) has NO successor in 26.2 RenderTypes (only debugFilledBox/debugPoint/debugQuads/debugTriangleFan; lines() is the nearest approximation), and LineStateShard(OptionalDouble) line width has no replay target on RenderSetupBuilder (fidelity loss only, not a crash).
- evidence: 26.2 and 26.1 javap RenderType: "static net.minecraft.client.renderer.rendertype.RenderType create(java.lang.String, net.minecraft.client.renderer.rendertype.RenderSetup);" (package-private, sole overload) and "private net.minecraft.client.renderer.rendertype.RenderType(java.lang.String, ...RenderSetup);". Vanilla construction (26.2 RenderTypes.lambda$static$6): "invokestatic RenderSetup.builder:(Lcom/mojang/blaze3d/pipeline/RenderPipeline;)...withTexture:(Ljava/lang/String;Lnet/minecraft/resources/Identifier;)...useLightmap...useOverlay...affectsCrumbling...setOutline...createRenderSetup:()Lnet/minecraft/client/renderer/rendertype/RenderSetup;" then "ldc String entity_cutout; invokestatic RenderType.create:(Ljava/lang/String;Lnet/minecraft/client/renderer/rendertype/RenderSetup;)Lnet/minecraft/client/renderer/rendertype/RenderType;". Cull flip (26.2 RenderPipelines <clinit>): "ldc String pipeline/entity_cutout ... iconst_0; invokevirtual RenderPipeline$Builder.withCull:(Z) ... putstatic ENTITY_CUTOUT" vs "ldc String pipeline/entity_cutout_cull ... build (no withCull) ... putstatic ENTITY_CUTOUT_CULL". Block layers: javap ChunkSectionLayer: "public static final ChunkSectionLayer SOLID/CUTOUT/TRANSLUCENT; public com.mojang.blaze3d.pipeline.RenderPipeline pipeline();" (returns pipeline, not RenderType). Epoch: unzip -Z1 of 26.1-snapshot-10 lists net/minecraft/client/renderer/rendertype/RenderType.class and NO RenderStateShard/CompositeState classes, but still MultiBufferSource.
